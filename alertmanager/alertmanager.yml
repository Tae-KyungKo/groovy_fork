global:
  resolve_timeout: 5m

route:
  receiver: "slack-notifications"

receivers:
  - name: "slack-notifications"
    slack_configs:
      - api_url_file: "/tmp/slack_webhook_url"
        channel: "#groovy-alert"
        send_resolved: true