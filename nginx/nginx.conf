# SSL/TLS 설정 이전의 nginx.conf 파일 내용
# server {
#     listen 80;
#     # 오류 발생하면 아래를 server_name _; 로 변경
#     server_name beancp.com;

#     location /api/ {
#         proxy_pass http://backend:8080;
#         proxy_http_version 1.1;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#     }

#     location / {
#         proxy_pass http://frontend:5173;
#         proxy_http_version 1.1;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

server {
    listen 443 ssl;
    server_name beancp.com;

    ssl_certificate /etc/letsencrypt/live/beancp.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/beancp.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;

    location /api/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto https;
    }

    location / {
        proxy_pass http://frontend:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# Lets Encrypt 인증서 발급을 위한 설정
server {
    listen 80;
    listen [::]:80;
    server_name beancp.com;
    access_log off;

    # Let's Encrypt 인증을 위한 예외 처리
    location /.well-known/acme-challenge/ {
        allow all;
        root /var/www/certbot;
    }

    # 나머지 요청은 HTTPS로 리디렉션
    location / {
        return 301 https://$host$request_uri;
    }
}