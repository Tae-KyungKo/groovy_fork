# 이미지 빌드에 로컬 산출물·로컬 환경설정이 섞여 들어가지 않게 한다.
# 특히 .env.local(VITE_API_BASE_URL=localhost)이 복사되면 Vite가 빌드 시
# 번들에 localhost 주소를 박아 넣어 배포 환경에서 API 호출이 전부 깨진다.
node_modules
dist
.env
.env.*
!.env.example
Dockerfile
.dockerignore
